---
name: Basic
route: /usage/basic
menu: Usage
---

import { Playground } from 'docz';

import ReactWordcloud from '../ReactWordcloud';
import {defaultOptions } from '../../src';
import words from './words';

# Basic Example

Simply provide an array of words with valid `text` and `value` fields to begin!

<Playground>
  <ReactWordcloud words={words} options={{...defaultOptions, rotations: -1 }}  />
</Playground>
